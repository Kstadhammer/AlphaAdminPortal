@{
    ViewData["Title"] = "Clients";
}

<partial name="Partials/Sections/_AddClientForm" />
<partial name="Partials/Sections/_EditClientForm" />
<div class="page-header">
    <h1>@ViewData["Title"]</h1>
    <button type="button" class="btn btn-add" data-modal="true" data-target="#addClientModal">Add Client</button>
</div>

<section id="clients">
    <div class="surface">
        @await Html.PartialAsync("Partials/Sections/_ClientList")
    </div>
</section>